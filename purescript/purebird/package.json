{
  "name": "purebird",
  "scripts": {
    "dev": "rm -rf dist && mkdir dist && cp public/* dist -r && node ./build & live-server dist"
  },
  "devDependencies": {
    "@types/node": "^16.4.3",
    "esbuild": "^0.12.9",
    "esbuild-plugin-alias": "^0.1.2",
    "esbuild-plugin-purescript": "^1.0.2",
    "esbuild-sass-plugin": "^1.4.7",
    "live-server": "^1.2.1",
    "sass": "^1.35.1",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "@thi.ng/matrices": "^0.6.59",
    "@thi.ng/vectors": "^6.0.1"
  }
}
